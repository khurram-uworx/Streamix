We have a IFlux contract defined in src\Streamix\IFlux.cs and src\Streamix\Implementations\FluxImplementation.cs and src\Streamix\Implementations\ConnectableStream.cs We have potentially duplicated code among these two implementations.

I am thinking we can adopt a minimal-interface + extension-method composition pattern similar to **Microsoft.Extensions.Logging** IFlux can be a small core interface that exposes only the essential primitive operations required for the feature and avoid adding convenience or high-level methods directly to the interface. Instead, implement all higher-level functionality, overloads, and ergonomic helpers as extension methods that compose over the core primitive. This keeps implementations lightweight and easy to maintain, ensures consistency of behavior across all implementations, and allows new features to be introduced without modifying or breaking the interface, aligning with the Open/Closed Principle from Object-Oriented Programming

I have started doing this by introduce src\Streamix\Extensions\FluxExtensions.cs and now I need your help to detect the methods from IFlux that are implemented in these two classes but have a duplicated code, the method potentially might be calling some private method and we should go ahead and check for duplication there as well. Just list down such methods for me and I will then self check them and we will fix the duplication issue elsewhere.
